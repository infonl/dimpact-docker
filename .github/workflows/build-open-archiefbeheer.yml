#
# SPDX-FileCopyrightText: 2026 INFO.nl
# SPDX-License-Identifier: EUPL-1.2+
#
name: Build open-archiefbeheer

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to build'
        required: false
        # renovate: datasource=docker depName=maykinmedia/open-archiefbeheer
        default: '2.0.0'
  push:
    branches:
      - main
    paths:
      - .github/workflows/build-open-archiefbeheer.yml

permissions:
  packages: write
  contents: read

jobs:
  determine-version:
    name: Determine version to build
    runs-on: ubuntu-24.04
    outputs:
      version: ${{ steps.resolve.outputs.version }}
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Resolve version to build
        id: resolve
        env:
          INPUT_VERSION: ${{ inputs.version }}
        run: |
          if [ -n "$INPUT_VERSION" ]; then
            version="$INPUT_VERSION"
          else
            version="$(grep -oP "^\s+default: '\K[^']+" .github/workflows/build-open-archiefbeheer.yml | head -n 1)"
          fi
          echo "Resolved version: $version"
          echo "version=$version" >> "$GITHUB_OUTPUT"

  build-arm64:
    name: Build arm64 image
    needs: determine-version
    runs-on: ubuntu-24.04-arm
    env:
      BUILD_VERSION: ${{ needs.determine-version.outputs.version }}
    steps:
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Check out open-archiefbeheer ${{ env.BUILD_VERSION }}
        id: checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: maykinmedia/open-archiefbeheer
          ref: ${{ env.BUILD_VERSION }}

      - name: Build open-archiefbeheer ${{ env.BUILD_VERSION }} for arm64
        run: |
          docker build \
            --tag ghcr.io/${{ github.repository_owner }}/open-archiefbeheer:${{ env.BUILD_VERSION }}-arm64 \
            --build-arg COMMIT_HASH=${{ steps.checkout.outputs.commit }} \
            --build-arg RELEASE=${{ env.BUILD_VERSION }} \
            .
          docker push ghcr.io/${{ github.repository_owner }}/open-archiefbeheer:${{ env.BUILD_VERSION }}-arm64

  build-amd64:
    name: Build amd64 image
    needs: determine-version
    runs-on: ubuntu-24.04
    env:
      BUILD_VERSION: ${{ needs.determine-version.outputs.version }}
    steps:
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Check out open-archiefbeheer ${{ env.BUILD_VERSION }}
        id: checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: maykinmedia/open-archiefbeheer
          ref: ${{ env.BUILD_VERSION }}

      - name: Build open-archiefbeheer ${{ env.BUILD_VERSION }} for amd64
        run: |
          docker build \
            --tag ghcr.io/${{ github.repository_owner }}/open-archiefbeheer:${{ env.BUILD_VERSION }}-amd64 \
            --build-arg COMMIT_HASH=${{ steps.checkout.outputs.commit }} \
            --build-arg RELEASE=${{ env.BUILD_VERSION }} \
            .
          docker push ghcr.io/${{ github.repository_owner }}/open-archiefbeheer:${{ env.BUILD_VERSION }}-amd64

  merge-manifest:
    name: Merge manifest list
    needs: [determine-version, build-amd64, build-arm64]
    runs-on: ubuntu-24.04
    env:
      BUILD_VERSION: ${{ needs.determine-version.outputs.version }}
    steps:
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4.5.1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create manifest
        run: |
          docker manifest create ghcr.io/${{ github.repository_owner }}/open-archiefbeheer:${{ env.BUILD_VERSION }} \
            ghcr.io/${{ github.repository_owner }}/open-archiefbeheer:${{ env.BUILD_VERSION }}-amd64 \
            ghcr.io/${{ github.repository_owner }}/open-archiefbeheer:${{ env.BUILD_VERSION }}-arm64
          docker manifest push ghcr.io/${{ github.repository_owner }}/open-archiefbeheer:${{ env.BUILD_VERSION }}
